# cli

Command-line interface for cloude backend communication.

## Overview

The cli component provides a command-line tool for communicating with the backend. It uses the clap library to parse arguments and send requests to backend.

## Installation

Build the CLI:

```bash
cargo build -p cli
```

The binary will be available at `target/debug/cli`.

## License

See workspace license for details.